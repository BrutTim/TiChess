apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: tichess
servers: 1
agents: 1
ports:
  - port: 8080:80
    nodeFilters:
      - loadbalancer
options:
  k3d:
    wait: true
    timeout: 120s
